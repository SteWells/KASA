// ================================================================
// KASA — LAUNCH SITE SUPPRESSION
// ================================================================
// Launch site hiding is handled by KASALaunchSiteHider in the
// KASA.dll plugin (KASADiscovery.cs), which runs at SpaceCentre
// startup and calls SetOpen(false) on hidden sites.
//
// MM patches for launch sites are not used — they are unreliable
// for stock sites and non-functional for Making History DLC sites
// which are instantiated at runtime rather than loaded from cfg.
// ================================================================
